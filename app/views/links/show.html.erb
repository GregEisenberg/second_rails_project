<p style="color: green"><%= notice %></p>

<%= render @link %>
<br/>
<div>
  <% if @link.user == current_user %>
    <%= link_to "Edit this link", edit_link_path(@link) %> |
    <%= link_to "Destroy this link", @link, method: :delete, data: { confirm: 'Are you sure?' }%>
  <% end %>
  
  <br/><br/>
  <%= link_to "Back to links", links_path %>

</div>
